#version: '4.0'
services:
  # --- SERVIÇO 1: BANCO DE DADOS ---
  web:
    container_name: arasolpoc
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: always
    ports:
      - "5000:5000"
